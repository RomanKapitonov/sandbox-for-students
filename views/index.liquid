<table>
  <tr>
   <th>First Name</th>
   <th>Last Name</th>
  </tr>
  {% for user in users %}
    <tr id="users">
      <td>{{ user.first_name }}</td>
      <td>{{ user.last_name }}</td>
    </tr>
  {% endfor %}
</table>
